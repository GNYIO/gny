version: '3.3'
services:
  postgres:
    image: "postgres:9.6.12"
    container_name: 'postgres_integration_test'
    restart: always
    ports:
      - '4000:5432'
    environment:
     POSTGRES_PASSWORD: docker
     POSTGRES_DB: postgres
     POSTGRES_USER: postgres
