version: 2
jobs:
  build:
    machine: true
    working_directory: ~/repo
    steps:
      # - setup_remote_docker
      - checkout
      - run:
          name: install-linux-dependencies
          command: sudo apt-get install curl sqlite3 ntp wget git libssl-dev openssl make gcc g++ autoconf automake python build-essential libtool -y
      - run:
          name: Install nvm and node@8.13
          command: |
            set +e  
            touch $BASH_ENV  
            curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.5/install.sh | bash
            echo 'export NVM_DIR="$HOME/.nvm"' >> $BASH_ENV
            echo '[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"' >> $BASH_ENV
            echo 'nvm install v8.13' >> $BASH_ENV
            echo 'nvm alias default v8.13' >> $BASH_ENV
      - run:
          name: npm-install-from-package-lock
          command: npm ci
      - run:
          name: test-integration
          command: npm run test:integration