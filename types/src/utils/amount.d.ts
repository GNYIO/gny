declare const _default: {
    validate(amount: any): "Invalid amount type" | "Amount should be integer" | "Failed to convert" | "Invalid amount range" | null;
    calcRealAmount(amount: any, precision: any): any;
};
export = _default;
