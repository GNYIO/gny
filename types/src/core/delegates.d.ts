export default class Delegate {
    private isLoaded;
    private blockStatus;
    private isForgingEnabled;
    private keyPairs;
    private library;
    constructor(scope: any);
    private loop;
}
